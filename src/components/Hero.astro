---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="relative bg-brand-maroon py-20 lg:py-32 overflow-hidden">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0 z-0">
    <img src={base + '/bg-gemini-2.png'} alt="Data Background" class="w-full h-full object-cover opacity-20 mix-blend-overlay" />
    <div class="absolute inset-0 bg-gradient-to-b from-brand-maroon/90 to-brand-maroon/70"></div>
  </div>

  <div class="container mx-auto px-4 relative z-10 text-center">
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6 leading-tight">
      Turning Data into <br class="hidden md:block" />
      <span class="text-brand-gold">Actionable Insight.</span>
    </h1>
    
    <p class="text-lg md:text-xl text-gray-200 max-w-2xl mx-auto mb-10 leading-relaxed">
      Specialized consulting in Economics Modeling, Causal Inference, Cost-Benefit Analysis, R&D Impact Evalutions, and Spatial Analysis.
    </p>
    
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a href={base + '/services'} class="inline-block bg-brand-gold text-brand-maroon font-bold px-8 py-3 rounded hover:bg-white hover:text-brand-maroon transition-all shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
        Learn More
      </a>
      <a href={base + '/contact'} class="inline-block border-2 border-white text-white font-semibold px-8 py-3 rounded hover:bg-white hover:text-brand-maroon transition-all">
        Contact
      </a>
    </div>
  </div>
  
  <!-- Decorative Background Elements -->
  <!-- Decorative Background Elements (Optional, kept subtle) -->
  <div class="absolute top-0 left-0 w-full h-full overflow-hidden z-0 opacity-20 pointer-events-none">
     <!-- Kept simple to let the image shine -->
  </div>
</section>
